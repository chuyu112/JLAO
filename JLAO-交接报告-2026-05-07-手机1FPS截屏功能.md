# JLAO 交接报告 — 2026-05-07（手机 1FPS 截屏功能）

## 本次新增目标

在已上线的 `scrcpy 原生窗口投屏` 基础上，新增一条稳定的手机画面采集链路：

```text
adb exec-out screencap -p
-> 后端压缩成 JPG
-> 复用现有 FrameSnapshot / 商品识别链路
-> WebSocket 推送给前端
-> 左侧 576×1024 手机屏区域显示最新帧
```

该方案不是实时视频流，而是 **1 秒 1 帧截屏**，优先保证稳定、低带宽、可识别。

---

## 已完成的本地代码改动

### 1. 后端：新增手机截屏服务

新增文件：

- `backend/app/services/phone_capture_service.py`

功能：

- 自动查找 `adb.exe`
  - `D:\scrcpy-win64-v3.3.4\adb.exe`
  - `C:\Program Files\scrcpy\adb.exe`
  - Chocolatey / Scoop 常见路径
  - PATH 中的 `adb`
- 每秒执行：

```cmd
adb -s 3AF9K24227080668 exec-out screencap -p
```

- 先保存原始 PNG，再压缩成 JPG。
- 原始 PNG 测试大小约：
  - `1.4MB`
- 压缩 JPG 测试大小约：
  - `49KB`
- 压缩参数：
  - 最大尺寸：`576×1024`
  - JPEG quality：`72`
  - optimize：`true`
  - progressive：`true`
- 压缩后调用现有：

```python
create_frame_snapshot(session_id, image_path, image_url)
```

因此会自动进入：

- 基础清晰度/亮度检测
- 商品识别
- WebSocket `frame_snapshot` 推送
- `app_state.frames`

---

### 2. 后端：新增 API

新增文件：

- `backend/app/api/phone_capture.py`

新增接口：

```text
POST /api/sessions/{session_id}/phone-capture/start
POST /api/sessions/{session_id}/phone-capture/stop
GET  /api/sessions/{session_id}/phone-capture/status
POST /api/sessions/{session_id}/phone-capture/once
```

请求体：

```json
{
  "serial": "3AF9K24227080668",
  "interval_seconds": 1
}
```

---

### 3. 后端：schema / main 接入

修改文件：

- `backend/app/schemas.py`
- `backend/app/main.py`

新增 schema：

- `PhoneCaptureStartRequest`
- `PhoneCaptureStatus`

新增路由注册：

```python
from app.api.phone_capture import router as phone_capture_router
app.include_router(phone_capture_router, prefix="/api", tags=["phone-capture"])
```

---

### 4. 前端：API / 类型 / Store 接入

修改文件：

- `frontend/src/types.ts`
- `frontend/src/api/jlao.ts`
- `frontend/src/stores/jlao.ts`

新增类型：

- `PhoneCaptureInfo`

新增 API：

- `startPhoneCapture`
- `stopPhoneCapture`
- `getPhoneCaptureStatus`

新增 Store 状态：

- `phoneCaptureInfo`
- `phoneCaptureLoading`

新增 Store action：

- `startPhoneCaptureSession(serial)`
- `stopPhoneCaptureSession()`
- `refreshPhoneCaptureStatus()`

停止手机端时会自动调用：

```ts
await this.stopPhoneCaptureSession()
```

---

### 5. 前端：投屏控制面板增加截屏按钮

修改文件：

- `frontend/src/components/ScrcpyPanel.vue`

新增按钮：

- `开始 1秒截屏`
- `停止截屏`

当前面板能力：

- 启动 scrcpy 原生投屏窗口
- 关闭投屏窗口
- 启动 1FPS adb 截屏
- 停止 1FPS adb 截屏

默认设备号：

```text
3AF9K24227080668
```

---

### 6. 前端：左侧手机屏显示最新帧

修改文件：

- `frontend/src/pages/LiveDashboard.vue`
- `frontend/src/styles/global.css`

完成内容：

- 左侧 `576×1024` 手机屏区域显示 `store.frames[0]`。
- 图片 URL 使用：

```ts
resolveAssetUrl(store.frames[0].image_path)
```

- 没有帧时显示占位文案：
  - `手机屏预留区`
  - `后续 1 秒 1 帧截屏会显示在这里`
- 有帧时显示：
  - 最新压缩 JPG 截图
  - `当前显示最新 1FPS 截屏`

---

## 已完成验证

### 1. 后端语法检查通过

执行：

```powershell
python -m py_compile backend\app\services\phone_capture_service.py backend\app\api\phone_capture.py backend\app\main.py backend\app\schemas.py
```

结果：通过。

---

### 2. 前端构建通过

执行：

```powershell
cd D:\jlao\frontend
npm.cmd run build
```

结果：

```text
✓ built
```

---

### 3. 实机 adb 截屏压缩测试通过

测试结果：

```text
raw 1399361
jpg 49016
size (460, 1024)
```

说明：

- adb 原始 PNG：约 `1.4MB`
- 压缩 JPG：约 `49KB`
- 已缩放到适配手机屏高度 `1024`

---

### 4. 后端服务级测试通过

执行过服务级测试：

- 创建临时 session
- 调用 `phone_capture_service.capture_once`
- 成功生成压缩 JPG
- 成功进入现有商品识别链路

测试输出示例：

```text
snap-8dd319a9c19d phone-7ba2a02e1f65.jpg 48754
```

说明：

- 生成 FrameSnapshot 成功
- 文件约 `48KB`
- 识别链路已触发

---

## 当前未完成

### 1. 还未部署到服务器

本次 1FPS 截屏功能只完成本地代码和验证，**尚未部署到服务器**。

### 2. 还未 Git commit / push

当前改动仍在本地工作区，尚未提交。

---

## 建议部署步骤

### 1. 确认本地构建

```powershell
cd D:\JLAO\frontend
npm.cmd run build
```

### 2. 部署服务器

如果部署脚本已由前一位修好，可执行：

```powershell
cd D:\JLAO
powershell -ExecutionPolicy Bypass -File deploy\deploy.ps1
```

部署目标：

```text
http://47.120.41.143
```

注意：

- 上一次部署卡点是包过大 / scp 上传慢。
- 如果再次卡住，需要确认部署包是否排除了：
  - `backend/.venv`
  - `frontend/node_modules`
  - `.release`
  - `tmp`
  - `uploads`

---

## 建议 Git 提交

建议提交信息：

```text
feat: add phone 1fps capture
```

命令：

```cmd
cd /d D:\JLAO
git add .
git commit -m "feat: add phone 1fps capture"
git push origin main
```

---

## 本次涉及文件清单

新增：

- `backend/app/api/phone_capture.py`
- `backend/app/services/phone_capture_service.py`
- `JLAO-交接报告-2026-05-07-手机1FPS截屏功能.md`

修改：

- `backend/app/main.py`
- `backend/app/schemas.py`
- `frontend/src/api/jlao.ts`
- `frontend/src/components/ScrcpyPanel.vue`
- `frontend/src/pages/LiveDashboard.vue`
- `frontend/src/stores/jlao.ts`
- `frontend/src/styles/global.css`
- `frontend/src/types.ts`

---

## 使用方式

1. 打开 JLAO Dashboard。
2. 点击 `载入手机端`。
3. 在左侧手机控制面板点击：
   - `启动投屏窗口`
4. scrcpy 原生窗口弹出后，再点击：
   - `开始 1秒截屏`
5. 左侧手机屏区域会显示最新手机截图。
6. 每秒截图会自动进入现有截图识别链路。

---

## 注意事项

1. 当前不是实时视频流，是 1FPS 截图。
2. 图片已压缩成 JPG，大小约几十 KB。
3. 该功能依赖本机 adb 能连接手机。
4. 云服务器本身无法直接连接本地 USB 手机，因此手机截屏功能主要适合本地后端运行场景。
5. 如果线上部署的是云服务器，而手机插在本地电脑，则需要后续再做“本地采集助手上传服务器”架构。

