# JLAO 直播截屏识别交接报告 - 2026-05-06

## 当前目标

本轮目标是把真实直播画面接入 JLAO 后，做到每 3 秒自动截取一帧，并把截屏图片放到页面独立卡片区域，后端对截图做基础画面识别，返回清晰度、亮度和画面状态。

## 已解决的问题

### 1. 真实直播音画接入

已在 JLAO 直播中控台左侧接入浏览器授权的直播画面。

当前流程：

1. 用户在浏览器打开抖音直播间。
2. 在 JLAO 点击“选择标签页接入音画”。
3. 浏览器弹出分享窗口。
4. 选择直播标签页，并尽量开启“同时分享标签页音频”。
5. JLAO 左侧播放直播画面和声音。

相关能力：

- 画面按 16:9 展示。
- 直播画面放在中控台左侧。
- 音频可通过浏览器标签页分享进入 JLAO。
- 用户可以将原直播标签页静音，只听 JLAO 内的视频声音，避免重声。

### 2. 真实语音转文字已跑通

已接入阿里云实时语音识别。

当前流程：

1. 前端从直播标签页获取音频。
2. Web Audio API 转成 16kHz PCM16。
3. 通过 JLAO 后端 WebSocket 发给阿里云实时 STT。
4. 阿里云返回实时识别文本。
5. JLAO 实时转写面板显示文字。

已修复的问题：

- 阿里云 STT 初始没有文字输出。
- 原因是启动请求缺少 `task_id` / `message_id`。
- 已修复握手协议，用户确认“可以了”。

注意：阿里云商用实时语音识别收费较高，用户反馈约 2.5 元/小时，后续应增加 STT 开关或更便宜的替代方案。

### 3. 模拟数据已清理

用户要求“把模拟文字、模拟画面、模拟音频都删了，直接玩真的”。

已处理：

- 前端不再走本地模拟登录/模拟直播 fallback。
- 后端不再自动模拟转写流。
- 截图识别走真实直播画面截图上传。
- 实时转写走真实音频 + 阿里云 STT。

### 4. 每 3 秒截屏识别链路已搭建

已完成真实截屏识别的前后端链路。

当前流程：

1. 用户接入直播画面。
2. 前端每 3 秒尝试截取当前直播画面。
3. 截图以 JPEG 上传到后端。
4. 后端保存图片到 `uploads/frames/{session_id}`。
5. 后端用 OpenCV 做基础分析。
6. 返回 `FrameSnapshot`。
7. 前端显示最近截图的画面识别结果、清晰度、亮度。
8. WebSocket 广播 `frame_snapshot` 事件。

后端识别内容：

- 清晰度：Laplacian variance。
- 亮度：灰度均值。
- 简单画面状态：清晰商品画面、可能偏糊、偏暗、偏亮等。

### 5. 截屏卡片区域已添加

用户要求“把截屏的图片放在一个页面卡片上”“单独准备一个区域放截屏”。

已完成：

- 新增 `FrameGalleryPanel.vue`。
- 页面增加独立“截屏卡片”区域。
- 区域放在直播主区域下方、底部统计栏上方。
- 每张卡片显示：
  - 截屏图片
  - 画面识别结果
  - 清晰度
  - 亮度
  - 截图时间

### 6. 截图图片可访问路径已接通

后端已把截图保存路径改成可被前端访问的 `/uploads/frames/...` URL。

同时 FastAPI 已挂载静态文件：

- `/uploads` -> 后端工作目录下的 `uploads`

这样前端卡片可以直接展示后端保存的截图图片。

### 7. 直播画质参数已提高

用户反馈 JLAO 内画面比原画面低清。

已调整：

- 浏览器采集目标从 1280x720 提高到 1920x1080。
- 截图识别最大宽度从 1280 提高到 1920。
- JPEG 质量从 0.82 提高到 0.92。

### 8. 已部署并推送 GitHub

本轮多次修改均已部署到线上环境：

- 线上地址：`https://jlao.szkakayiduo.com/live`
- 后端服务：`jlao-backend`
- Nginx：active
- `/health`：OK

相关提交包括：

- `2b8c15e Add live frame recognition`
- `d285947 Improve live capture quality.`
- `57d2631 Add live frame screenshot cards.`
- `5046f0d Show screenshot cards below dashboard.`
- `b630101 Fix black live frame screenshots.`
- `16832c0 Skip black live frame uploads.`

当前代码已推送到 GitHub `main`。

## 还没解决的问题

### 核心未解决：截屏结果仍然是黑屏

用户测试结果：

- 截屏卡片区域已经出现。
- 但截出来的图片仍然是黑屏。

已经尝试过的修复：

1. 从 `<video>` 元素 canvas 截图。
2. 改用 `ImageCapture.grabFrame()` 直接从视频轨道抓帧。
3. 增加黑屏检测，黑图不上传。
4. 如果 ImageCapture 黑屏，再退回 video canvas。
5. 如果还是黑屏，前端提示“当前截图仍是黑屏，已跳过上传”。

但用户最终反馈仍是黑屏。

### 当前判断

这个问题很可能不是后端保存或 OpenCV 识别的问题，而是浏览器分享出来的视频帧本身就不可正常截取。

可能原因：

1. 抖音直播网页可能对画面做了保护或特殊渲染。
2. 浏览器捕获的 `<video>` 预览能播放，但 canvas/ImageCapture 读取时拿到黑帧。
3. Quark 浏览器的屏幕/标签页分享实现可能和 Chrome 不完全一致。
4. 直播视频可能使用硬件加速、DRM、跨进程渲染或受保护图层，导致前端截图 API 抓不到真实像素。
5. 当前选择的是“标签页”源，浏览器可能允许显示和播放，但不允许二次 canvas 抽帧。

## 未来准备怎么解决

### 方案 A：换采集源测试，确认是不是浏览器/标签页限制

下一步优先做这个，因为成本最低。

测试路径：

1. 不选“标签页”，改选“窗口”或“整个屏幕”。
2. 让直播画面在单独浏览器窗口中播放。
3. JLAO 捕获整个窗口或整个屏幕。
4. 再看截图卡片是否仍然黑屏。

判断：

- 如果窗口/屏幕模式能截到图，说明标签页模式受限。
- 如果所有模式都黑屏，说明该浏览器/直播源对截图限制更强。

### 方案 B：要求使用 Chrome/Edge 测试，不优先使用 Quark

Quark 浏览器的标签页分享源之前已经和 Chrome 表现不同。

建议下一次测试：

1. 用最新版 Chrome 或 Edge 打开抖音直播间。
2. 用 JLAO 选择 Chrome/Edge 的直播标签页。
3. 开启标签页音频分享。
4. 测试截图是否黑屏。

判断：

- 如果 Chrome/Edge 正常，说明 Quark 不适合作为采集浏览器。
- 如果 Chrome/Edge 也黑屏，再走方案 C/D。

### 方案 C：改用本地屏幕采集服务，而不是浏览器前端截图

如果浏览器前端拿不到真实像素，下一步应把截图能力移到本机/服务器侧采集程序。

可选实现：

- Windows 本地小程序截屏。
- Python + MSS / pyautogui 截指定区域。
- OBS 虚拟摄像头/截图输出。
- Electron 桌面端使用 desktopCapturer。

推荐方向：

- 做一个 JLAO 本地采集助手。
- 用户在助手里框选直播区域。
- 助手每 3 秒截屏上传 JLAO 后端。
- JLAO 页面只负责显示识别结果和截图卡片。

优点：

- 绕过浏览器标签页 canvas 限制。
- 可以截任意屏幕区域。
- 更适合直播中控实际使用。

缺点：

- 需要安装本地程序或运行本地脚本。
- 部署复杂度高一点。

### 方案 D：直接接入直播流地址

如果能拿到抖音直播真实播放流地址，可以后端直接抽帧。

流程：

1. 获取直播流 URL。
2. 后端用 ffmpeg/OpenCV 读取流。
3. 每 3 秒抽帧。
4. 后端识别并推送前端。

优点：

- 最稳定。
- 不依赖浏览器截图。
- 可以后台自动跑。

缺点：

- 抖音直播流地址可能有鉴权、时效和反爬限制。
- 合规和稳定性需要评估。

### 方案 E：临时保留手动上传截图

为了不阻塞后续 AI 识别能力，可以先加一个手动上传截图入口。

流程：

1. 用户手动截屏。
2. 上传到 JLAO。
3. 后端识别。
4. 页面展示卡片。

优点：

- 快速验证后端识别和卡片展示完全正常。
- 不依赖直播采集限制。

缺点：

- 不是自动化方案。
- 只适合临时调试。

## 建议下一步顺序

推荐明天按这个顺序排查：

1. 先用 Chrome/Edge 重测当前标签页采集。
2. 再测试“窗口/整个屏幕”分享模式。
3. 如果仍黑屏，暂停前端截图方案。
4. 做本地采集助手或 Electron 桌面端截图。
5. 同时保留当前 JLAO 截图卡片和后端识别接口，因为这部分可以复用。

## 当前可复用成果

即使黑屏问题未解决，以下模块已经可以继续复用：

- 前端截图卡片 UI。
- 前端 frames 状态管理。
- 后端截图上传接口。
- 后端图片静态访问。
- 后端 OpenCV 基础识别。
- WebSocket 推送 `frame_snapshot`。
- 页面展示清晰度、亮度、画面状态。

未来只需要替换“截图来源”，不需要重做整个识别展示链路。

## 结论

本轮已经完成了直播音画接入、实时 STT、每 3 秒截图识别链路、截图卡片展示、部署和 GitHub 推送。

当前唯一核心卡点是：浏览器前端从直播分享流截出来仍是黑屏。

下一步不建议继续在同一个前端 canvas 截图方案上硬试太久，应该优先验证 Chrome/Edge 与“窗口/整屏”分享模式。如果仍然黑屏，就切换到本地采集助手或 Electron 桌面端截图方案。
