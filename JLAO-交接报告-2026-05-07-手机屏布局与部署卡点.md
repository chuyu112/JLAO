# JLAO 交接报告 — 2026-05-07（手机屏布局与部署卡点）

## 当前目标

在 JLAO 控制台最左侧预留一个手机屏位置，后续用于展示“手机端视频流截屏 / 1 秒 1 帧”的画面；同时将原来的“开始直播”入口改为“载入手机端”。

## 已完成的本地改动

### 1. 左侧手机屏预留区

修改文件：

- `frontend/src/pages/LiveDashboard.vue`
- `frontend/src/styles/global.css`

完成内容：

- 在主控制台 `main-grid` 最左侧新增手机屏区域。
- 手机屏尺寸按 9:16 竖屏预留：
  - 宽度：`576px`
  - 高度：`1024px`
- 目前显示占位文案：
  - `手机屏预留区`
  - `后续 1 秒 1 帧截屏会显示在这里`
- scrcpy 控制面板移动到手机屏下方。

### 2. 顶部按钮文案调整

修改文件：

- `frontend/src/components/SessionStatusBar.vue`

完成内容：

- 将原来的“开始直播”按钮改为：
  - `载入手机端`
- 状态文案做了兼容处理：
  - 直播中 / 旧乱码状态都尽量识别为已载入状态。

### 3. scrcpy 原生窗口控制方案

修改文件：

- `backend/app/services/scrcpy_service.py`
- `frontend/src/components/ScrcpyPanel.vue`
- `frontend/src/pages/LiveDashboard.vue`
- `frontend/src/stores/jlao.ts`

完成内容：

- 后端通过 `scrcpy.exe` 启动原生投屏窗口。
- 前端投屏面板默认设备号：
  - `3AF9K24227080668`
- 当前已经实测：本地 scrcpy 投屏可用。

### 4. 后端启动脚本修复

修改文件：

- `scripts/start-backend.ps1`

完成内容：

- 当前 `backend/.venv` 的 Python 是坏的，会报：
  - `uv trampoline failed to spawn Python child process`
- 启动脚本已改为：
  - 如果 `.venv` Python 坏了，自动回退到系统 Python。
- 同时移除了 `uvicorn --reload`，避免 Windows 环境下触发命名管道权限错误。

## 已验证

### 前端构建

已执行并通过：

```powershell
cd D:\jlao\frontend
npm.cmd run build
```

结果：

- `vue-tsc -b` 通过
- `vite build` 通过

### 后端启动

已验证脚本可启动后端：

```cmd
cd /d D:\JLAO
powershell -ExecutionPolicy Bypass -File scripts\start-backend.ps1
```

成功标志：

```text
Uvicorn running on http://127.0.0.1:8000
```

## 当前部署状态

### 部署未完成

执行过：

```powershell
powershell -ExecutionPolicy Bypass -File deploy\deploy.ps1
```

流程走到：

```text
[JLAO] Uploading package to server...
Warning: Permanently added '47.120.41.143' ...
```

之后 `scp` 上传阶段长时间无输出，部署未成功完成。

### 已处理的部署问题

修改文件：

- `deploy/deploy.ps1`

已加：

```powershell
-o StrictHostKeyChecking=accept-new
```

用于避免首次 SSH 主机指纹校验卡住 / 报：

```text
Host key verification failed
```

### 当前部署卡点判断

怀疑部署包过大，原因是当前部署脚本复制了整个 `backend`，其中包含：

- `backend/.venv`

这会显著增大 `jlao-release.tar.gz`，导致 `scp` 上传慢或卡住。

## 建议下一步

### 优先级 1：优化部署包

修改 `deploy/deploy.ps1`，打包时排除：

- `backend/.venv`
- `backend/__pycache__`
- `backend/app/**/__pycache__`
- `.release`
- `tmp`
- `uploads`
- 其他不需要上传的本地缓存

推荐只上传：

- `backend/app`
- `backend/requirements.txt`
- `data`
- `frontend/dist`
- `deploy/server-install.sh`

然后重新部署：

```powershell
powershell -ExecutionPolicy Bypass -File deploy\deploy.ps1
```

### 优先级 2：部署成功后 Git 提交并推送

当前尚未提交，也尚未 push。

建议提交信息：

```text
feat: add phone screen stage and native scrcpy control
```

命令：

```cmd
git add .
git commit -m "feat: add phone screen stage and native scrcpy control"
git push origin main
```

## 当前本地改动文件清单

主要改动：

- `backend/app/services/scrcpy_service.py`
- `deploy/deploy.ps1`
- `frontend/src/components/ScrcpyPanel.vue`
- `frontend/src/components/SessionStatusBar.vue`
- `frontend/src/pages/LiveDashboard.vue`
- `frontend/src/stores/jlao.ts`
- `frontend/src/styles/global.css`
- `scripts/start-backend.ps1`

新增文档：

- `JLAO-交接报告-2026-05-07-scrcpy-原生窗口控制方案.md`
- `JLAO-交接报告-2026-05-07-手机屏布局与部署卡点.md`

## 注意事项

1. 当前线上服务器还不是最新版本。
2. 本地前端已经是最新版本且构建通过。
3. 下一步不要直接再次上传大包，建议先优化部署脚本。
4. 手机屏区域目前只是 UI 占位，尚未接入 1 秒 1 帧截屏。

