# JLAO 架构说明

## 架构设计（最终版）

```
┌─────────────────────────────────────────┐
│           用户本地电脑                     │
│                                         │
│  ┌─────────────┐    ┌─────────────┐   │
│  │   浏览器     │◄──►│   本地后端    │   │
│  │             │    │ （Python）   │   │
│  └─────────────┘    └──────┬──────┘   │
│                             │          │
│  ┌──────────────────────────┼──────┐  │
│  │      USB 投屏（scrcpy）    │      │  │
│  │      语音转写（FunASR）◄───┘      │  │
│  │      图像 OCR（PaddleOCR）         │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│        服务器（47.120.41.143）            │
│                                         │
│     ┌─────────────┐    ┌───────────┐   │
│     │   Nginx     │    │  数据库    │   │
│     │  （前端）    │    │（SQLite）  │   │
│     └─────────────┘    └───────────┘   │
└─────────────────────────────────────────┘
```

## 组件说明

| 组件 | 位置 | 技术 | 说明 |
|------|------|------|------|
| **前端** | 服务器 | Nginx + Vue.js | 用户界面，通过 HTTPS 访问 |
| **后端** | 本地电脑 | Python FastAPI | 处理业务逻辑、USB 投屏、语音转写、图像 OCR |
| **数据库** | 服务器 | SQLite | 存储数据，后端通过 HTTP 连接 |
| **USB 投屏** | 本地电脑 | scrcpy | 连接手机投屏 |
| **语音转写** | 本地电脑 | FunASR | 实时语音转文字 |
| **图像 OCR** | 本地电脑 | PaddleOCR | 实时图像文字识别 |

## 数据流

1. **用户** → 浏览器 → 前端（服务器）
2. **前端** → API 请求 → 本地后端
3. **本地后端** → 处理 USB 投屏、语音转写、图像 OCR
4. **本地后端** → 存储数据 → 服务器数据库

## 启动方式

### 方案 2：服务器前端 + 本地后端（推荐）

启动本地后端：

```bash
cd D:\JLAO\backend
.venv\Scripts\activate
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

访问：
```
https://jlao.szkakayiduo.com/live?api=http://127.0.0.1:8000
```

**注意**：需要处理 HTTPS → HTTP 的混合内容问题

## 数据库配置

### 服务器环境

数据库位置：`http://47.120.41.143/data/jlao-mvp.sqlite`

需要配置环境变量：
```bash
set DATABASE_URL=http://47.120.41.143/data/jlao-mvp.sqlite
```

## 注意事项

1. **本地环境**：所有服务在本地运行，无需网络
2. **服务器环境**：前端在服务器，后端在本地或服务器
3. **混合环境**：前端在服务器，后端在本地，数据库在服务器

## 推荐配置

**生产部署**：使用服务器前端 + 本地后端（方案 2）
