# JLAO 交接报告 - 2026-05-06

项目：Jade Live AI Optimizer（JLAO）
中文名：翡翠直播间实时 AI 优化系统
当前阶段：MVP 第 1 天 / 基础闭环启动
线上地址：https://jlao.szkakayiduo.com
备用 IP：http://47.120.41.143

## 1. 今日结论

今天已经完成 JLAO 从 0 到可访问 Demo 的第一版闭环。

当前系统已经具备：

```text
登录
-> 创建直播会话
-> 选择翡翠商品
-> 模拟直播转写
-> AI 建议卡片
-> 直播画面窗口采集
-> 每 3 秒截屏上传
-> OpenCV 基础图像分析
-> HTTPS 公网访问
```

最重要的是：**画面接入路线已经跑通**。

运营可以在 Chrome 登录抖音直播间，然后在 JLAO 里点击“选择 Chrome 窗口”，由浏览器授权选择采集哪个窗口。JLAO 每 3 秒截一帧上传后端，后端用 OpenCV 做基础图像检测。

## 2. 今日完成事项

### 2.1 产品与文档

已完成：

- 中文 PRD
- 中文技术路线
- 项目启动计划
- 文档索引
- 一个月 MVP 目标确认
- 中文优先原则确认
- UI 风格确认：专业、高效、节奏快、类似直播中控大屏

相关文档：

```text
Jade Live AI Optimizer - 中文PRD.md
JLAO-技术路线与MVP计划.md
JLAO-项目启动计划.md
JLAO-文档索引.md
```

### 2.2 前端

技术栈：

```text
Vue 3
Vite
TypeScript
Pinia
Naive UI
WebSocket
```

已完成：

- 登录页
- 登录状态保存
- 路由守卫
- 顶部用户角色显示
- 深色直播中控大屏风格
- 直播中控台页面
- 商品库页面
- 复盘页
- 当前商品卡
- 实时转写面板
- AI 建议中心
- 直播画面模块
- 直播链接保存
- iframe 预览尝试
- Chrome 窗口采集
- 每 3 秒截图上传
- 采集状态显示
- OpenCV 分析结果显示：画面状态、清晰度、亮度

登录账号：

```text
operator / jlao123
anchor / jlao123
admin / jlao123
```

### 2.3 后端

技术栈：

```text
Python 3.11
FastAPI
Pydantic
WebSocket
OpenCV headless
Pillow
```

已完成接口：

```text
GET  /health
POST /api/auth/login
GET  /api/products
POST /api/sessions
POST /api/sessions/{id}/start
POST /api/sessions/{id}/stop
POST /api/sessions/{id}/current-product/{product_id}
POST /api/sessions/{id}/live-url
GET  /api/sessions/{id}/transcripts
GET  /api/suggestions/sessions/{session_id}
POST /api/suggestions/{id}/accept
POST /api/suggestions/{id}/copy
POST /api/suggestions/{id}/used
POST /api/suggestions/{id}/reject
POST /api/sessions/{id}/replay
POST /api/sessions/{id}/frames/upload
GET  /api/sessions/{id}/frames
WS   /ws/sessions/{session_id}
```

已完成后端能力：

- Demo 登录
- 商品样例加载
- 直播会话管理
- 模拟直播转写流
- 简单规则型 AI 建议
- 风险提醒规则
- 复盘报告 v0
- 图片上传
- OpenCV 基础图像分析
- WebSocket 推送

### 2.4 部署

服务器：

```text
IP：47.120.41.143
系统：Alibaba Cloud Linux 3
部署目录：/opt/jlao
前端目录：/var/www/jlao
后端服务：jlao-backend
```

已完成：

- SSH Key 登录配置
- 安装 Python 3.11
- 安装后端依赖
- 配置 systemd 后端服务
- 配置 Nginx
- 绑定域名 jlao.szkakayiduo.com
- 签发 HTTPS 证书
- HTTP 自动跳转 HTTPS
- 配置 WebSocket 代理
- 配置 API 代理
- 部署脚本

证书：

```text
域名：https://jlao.szkakayiduo.com
证书路径：/etc/letsencrypt/live/jlao.szkakayiduo.com/
到期时间：2026-08-03
自动续期：已启用
```

部署脚本：

```text
deploy/deploy.ps1
deploy/server-install.sh
deploy/nginx-jlao.conf
deploy/jlao-backend.service
```

### 2.5 线上验证

已验证：

```text
https://jlao.szkakayiduo.com                 OK
https://jlao.szkakayiduo.com/health          OK
https://jlao.szkakayiduo.com/api/auth/login  OK
https://jlao.szkakayiduo.com/api/products    OK，返回 5 个商品
帧上传 /frames/upload                         OK
OpenCV 基础分析                              OK
```

最后一次帧上传验证结果：

```text
detected_scene: 画面可能偏糊
sharpness_score: 0.0
brightness_score: 131.0
```

说明上传链路、后端存图、OpenCV 处理已经跑通。

## 3. 当前画面接入方案

当前采用浏览器屏幕采集方案。

使用方式：

```text
1. 用 Chrome 打开抖音直播间
2. 登录抖音账号
3. 打开 https://jlao.szkakayiduo.com
4. 登录 JLAO
5. 进入中控台
6. 点击“选择 Chrome 窗口”
7. 在浏览器弹出的系统窗口选择器里选择抖音直播窗口或标签页
8. JLAO 每 3 秒截屏一次并上传后端分析
```

为什么这样做：

- 抖音拒绝 iframe 嵌入，这是平台安全策略
- JLAO 不保存抖音账号密码
- 抖音登录、验证码、权限由 Chrome 自己处理
- JLAO 只采集用户授权的窗口画面
- HTTPS 已配置，浏览器允许 getDisplayMedia 采集

当前限制：

- 必须由用户手动选择窗口
- 不能自动猜测哪个是抖音窗口
- 当前只做基础 OpenCV 分析，还没有接多模态模型识别商品
- 当前没有采集声音

## 4. 今天没做完的事项

### 4.1 真实语音识别

当前状态：

- 还没接真实 STT
- 现在是模拟直播文本流

待做：

```text
录音文件转写
实时麦克风/系统音频转写
语音分段
翡翠术语纠错
```

模型方案待定：

```text
云端方案：OpenAI / 阿里云 / 火山 STT，按时长或 token 计费
本地方案：faster-whisper，不花 API token，但吃本机/服务器算力
```

### 4.2 真正的大模型 Agent

当前状态：

- 现在是规则型建议和固定话术
- 还没有接真实 LLM

待做：

```text
商品讲解 Agent
漏讲提醒 Agent
场控互动 Agent
用户答疑 Agent
合规 Agent
复盘 Agent
```

### 4.3 图像智能识别

当前状态：

- OpenCV 只做基础清晰度、亮度、状态检测

待做：

```text
多模态模型识别直播画面
识别是否在展示手镯/挂件/证书/上手效果
识别画面是否切到数据大屏
OCR 识别价格、在线人数、评论数
画面变化检测
每 2-5 秒生成画面摘要
```

### 4.4 数据持久化

当前状态：

- 后端使用内存状态
- 重启后直播会话、转写、建议会丢失

待做：

```text
PostgreSQL 接入
SQLAlchemy 模型
数据库迁移
直播会话持久化
商品资料持久化
建议和反馈持久化
帧记录持久化
复盘报告持久化
```

### 4.5 账号权限

当前状态：

- Demo 登录账号
- 没有真实用户系统

待做：

```text
密码加密
JWT
角色权限
直播间权限
操作审计
管理员后台
```

### 4.6 前端体验

当前状态：

- 已有中控大屏雏形
- 还需要进一步贴近直播场控真实工作流

待做：

```text
更紧凑的指标区
建议卡优先级排序
风险提醒固定置顶
直播画面和建议区比例优化
按钮快捷操作
移动端暂不考虑
```

## 5. 明天建议优先级

### P0：把真实语音接进来

目标：

```text
主播声音 -> 实时文字 -> AI 建议
```

建议先做：

```text
录音文件转写
再做实时麦克风转写
最后做系统音频采集
```

### P0：接一个真实 LLM 建议接口

目标：

```text
当前商品 + 最近转写 + 画面分析 -> 生成真实 AI 建议
```

先接一个统一 LLM service，不急着上复杂多 Agent。

### P1：画面摘要

目标：

```text
每 3 秒截图
-> 多模态模型生成一句画面摘要
-> 显示在中控台
```

示例：

```text
当前画面：主播正在展示一只手镯上手效果，画面偏亮，建议补充自然光说明。
```

### P1：PostgreSQL

目标：

```text
数据不要再只放内存
```

优先持久化：

```text
products
live_sessions
transcript_segments
suggestions
frame_snapshots
```

## 6. 当前风险

### 6.1 公网截图采集依赖浏览器授权

浏览器不会允许网页静默截屏，必须用户手动选择窗口。这是安全机制，不能绕。

### 6.2 直播平台限制 iframe

抖音 `live.douyin.com` 会拒绝 iframe 连接。当前已改为窗口采集路线。

### 6.3 当前 AI 还不是真智能

现在建议生成是规则型，不是大模型实时推理。明天必须接 LLM，否则产品价值还只是壳。

### 6.4 数据未持久化

服务重启会丢数据。Demo 可接受，但试用不可接受。

### 6.5 服务器 root 密码已暴露过

建议后续：

```text
更换 root 密码
创建 deploy 用户
禁用密码登录
只保留 SSH Key
```

## 7. 运行与部署命令

本地前端：

```powershell
.\scripts\start-frontend.ps1
```

本地后端：

```powershell
.\scripts\start-backend.ps1
```

部署：

```powershell
.\deploy\deploy.ps1
```

服务器状态检查：

```bash
systemctl status jlao-backend
systemctl status nginx
curl http://127.0.0.1:8000/health
```

## 8. 交接给明天的第一步

明天第一件事建议做：

```text
接真实语音转文字，先用录音文件转写打通。
```

第二件事：

```text
接真实 LLM，根据商品 + 转写 + 截图分析生成直播建议。
```

今天已经把“壳、域名、HTTPS、登录、商品、模拟转写、截图采集、OpenCV 基础分析、部署链路”全部打通。

可以收工。

