# JLAO 项目启动计划

日期：2026-05-05
版本：v0.1
项目：Jade Live AI Optimizer（JLAO）
中文名称：翡翠直播间实时 AI 优化系统
目标：一个月内完成可演示 MVP Demo

## 1. 启动结论

JLAO 从今天开始进入 MVP 开发启动阶段。

当前已经确定：

- 产品方向：翡翠直播间实时 AI 优化系统
- 产品形态：直播 AI 场控副驾
- 使用人群：主播、场控、客服、运营、货主
- 语言策略：中文优先
- 执行边界：AI 建议，真人审核执行
- MVP 底线：一个月内必须完成可演示 Demo

## 2. 技术栈冻结

一个月 MVP 阶段不再反复比较技术栈，直接按以下方案执行。

### 2.1 前端

```text
Vue 3
Vite
TypeScript
Pinia
Vue Router
Naive UI
ECharts
WebSocket
Axios
```

前端定位：

- 实时中控台
- 商品库
- AI 建议审核台
- 复盘报告页
- 登录和角色入口
- 直播中控大屏风格

### 2.2 后端

```text
Python
FastAPI
Pydantic
SQLAlchemy
PostgreSQL
WebSocket
FFmpeg
OpenCV
Pillow
```

后端定位：

- 直播会话管理
- 商品管理
- 转写流管理
- 上下文管理
- AI 建议生成
- 合规检查
- 复盘生成
- 直播画面抽帧和预处理

### 2.3 视频处理

```text
FFmpeg
OpenCV
Pillow
OCR 引擎，后续可选
多模态大模型，后续可选
```

视频处理定位：

- 直播窗口截图
- 每 2-5 秒抽帧
- 裁剪商品展示区
- 裁剪数据大屏区
- 检测画面清晰度
- 检测亮度和曝光
- 检测画面变化
- OCR 前图像增强
- 多模态识别前图像预处理

MVP 优先级：

```text
第 1 周：不强依赖 OpenCV，先跑通模拟文本流
第 2 周：预留 FrameSnapshot 数据结构和接口
第 3 周：加入截图上传或本地图片分析
第 4 周：尝试 OpenCV 抽帧和基础画面检测
Demo 后：完善 OCR、商品识别、数据大屏识别
```

### 2.4 暂缓引入

一个月 MVP 前暂缓：

```text
Redis
pgvector
LangGraph
Docker 生产部署
多手机 Agent
数据大屏 OCR
复杂截图识别，但保留 OpenCV 抽帧和预处理能力
模型 fine-tune
多直播间
复杂权限系统
```

原因：

- 这些能力有价值，但不是第一个 Demo 的核心证明点。
- 一个月目标要求优先跑通业务闭环。

## 3. MVP 核心闭环

第一版只验证这个闭环：

```text
登录直播间
-> 创建直播
-> 选择翡翠商品
-> 模拟或转写直播内容
-> AI 生成建议
-> 人工审核建议
-> 标记使用结果
-> 生成复盘报告
```

只要这个闭环稳定，JLAO 的产品价值就成立。

## 4. 一个月路线图

### 第 1 周：能跑起来

目标：

- 前端能打开
- 后端能启动
- 能创建直播
- 能选择商品
- 能显示模拟直播文本流
- WebSocket 能推送实时内容
- 能登录进入中控台

交付物：

```text
直播中控台 v0
商品库 v0
登录页 v0
模拟直播转写流
WebSocket 推送
10 个翡翠商品样例
30 条翡翠直播问答样例
```

验收标准：

```text
打开页面 -> 登录 -> 创建直播 -> 选择商品 -> 点击开始 -> 左侧实时出现模拟转写
```

### 第 2 周：AI 建议可用

目标：

- 能根据当前商品和直播文本生成 AI 建议
- 能显示建议卡片
- 能进行人工审核
- 能做基础合规检查

交付物：

```text
商品讲解建议
专业漏讲提醒
合规风险提醒
建议卡片
复制/编辑/接受/拒绝/已使用
```

验收标准：

```text
主播讲颜色时，AI 能提醒补圈口、证书、瑕疵、自然光。
出现“保证升值”等话术时，AI 能提示风险并给出改写。
```

### 第 3 周：更像真实直播工具

目标：

- 增加用户答疑建议
- 增加场控互动建议
- 增加简单成交节奏建议
- 接入至少一种真实或半真实转写方式
- 接入 OpenCV 截图上传或图片分析原型

交付物：

```text
用户答疑 Agent
气氛互动 Agent
成交节奏 Agent v0
录音文件转写或实时转写原型
OpenCV 图片预处理 v0
上下文时间线
```

验收标准：

```text
用户问“有裂吗、自然光会不会灰、还能便宜吗”时，AI 能给出可用中文回答。
```

### 第 4 周：Demo 打磨和复盘

目标：

- 完整演示一场翡翠直播
- 生成复盘报告
- 调整 UI
- 调整话术
- 减少重复建议
- 准备演示脚本

交付物：

```text
复盘报告
Demo 数据集
Demo 演示脚本
稳定演示流程
```

验收标准：

```text
10 分钟内完整演示：
创建直播 -> 选择商品 -> 输入直播内容 -> AI 建议 -> 人工审核 -> 结束直播 -> 生成复盘
```

## 5. 第 1 周详细启动计划

### Day 1：项目骨架

目标：

- 创建前后端项目
- 确定目录结构
- 跑通前后端启动

任务：

```text
创建 frontend 目录
创建 backend 目录
初始化 Vue 3 + Vite + TypeScript
初始化 FastAPI 项目
创建 README
创建基础配置文件
前端显示 JLAO 首页
后端提供 /health 接口
```

验收：

```text
前端页面能打开
后端 /health 返回正常
```

### Day 2：直播会话和商品模型

目标：

- 建立核心数据模型
- 能创建直播和商品

任务：

```text
设计 LiveSession 模型
设计 Product 模型
实现直播会话 API
实现商品 API
准备 10 个商品样例
前端商品库页面 v0
```

验收：

```text
能新增商品
能创建直播
能在中控台选择当前商品
```

### Day 3：中控台主界面

目标：

- 建立直播中控台信息架构

任务：

```text
实现顶部直播状态栏
实现实时转写面板
实现 AI 建议面板空状态
实现当前商品卡
实现风险提醒区域
```

验收：

```text
页面布局稳定，所有核心区域都有位置。
```

### Day 4：模拟直播转写流

目标：

- 先用模拟文本跑通实时流

任务：

```text
准备模拟翡翠直播文本
实现后端模拟转写服务
实现 WebSocket 推送
前端接收 transcript_segment 事件
实时追加到转写面板
```

验收：

```text
点击开始直播后，左侧转写内容逐句出现。
```

### Day 5：上下文引擎 v0

目标：

- 把最近直播文本和当前商品组合成 AI 可用上下文

任务：

```text
保存 transcript_segments
维护最近 30-120 秒文本
维护当前商品信息
预留 FrameSnapshot 数据结构
生成 context_block
为第 2 周 AI 建议做准备
```

验收：

```text
后端能输出当前直播上下文：当前商品 + 最近转写 + 关键词。
```

### Day 6：样例数据和中文体验

目标：

- 准备 Demo 所需中文数据

任务：

```text
补齐 10 个翡翠商品样例
补齐 30 条直播问答样例
补齐 20 条风险话术样例
检查前端文案全部中文
检查按钮和状态名称是否符合一线用户习惯
```

验收：

```text
Demo 数据能覆盖手镯、挂件、珠串、蛋面、雕件等常见场景。
```

### Day 7：第 1 周集成验收

目标：

- 完成第一周闭环

任务：

```text
跑一遍完整流程
修复明显问题
整理第 2 周 AI Agent Prompt
准备商品讲解 Agent、漏讲 Agent、合规 Agent 初稿
```

验收：

```text
能演示：
创建直播 -> 选择商品 -> 开始模拟转写 -> 中控台实时显示。
```

## 6. 目录结构建议

```text
JLAO/
  docs/
    中文PRD.md
    技术路线与MVP计划.md
    项目启动计划.md
  frontend/
    src/
      pages/
      components/
      stores/
      api/
      types/
  backend/
    app/
      api/
      models/
      schemas/
      services/
      ws/
      prompts/
      seed/
    tests/
  data/
    samples/
      products.json
      questions.json
      risk_phrases.json
      mock_transcripts.json
```

## 7. 首批样例数据要求

### 7.1 商品样例

必须覆盖：

```text
手镯
挂件
珠串
蛋面
雕件
```

至少 10 个商品。

每个商品至少包含：

```text
名称
分类
颜色
种水
尺寸
证书
瑕疵
价格
卖点
注意事项
常见问题
```

### 7.2 直播问答样例

至少 30 条，覆盖：

```text
证书
A 货
纹裂
棉
色差
自然光
圈口
价格
售后
发货
适合人群
对比选择
```

### 7.3 风险话术样例

至少 20 条，覆盖：

```text
升值承诺
投资承诺
绝对化表达
虚假稀缺
虚假低价
功效迷信
过度承诺
```

## 8. Demo 版本验收清单

一个月 MVP Demo 必须满足：

```text
能创建直播
能选择商品
能显示实时转写
能生成主播话术建议
能生成漏讲提醒
能生成场控互动建议
能生成用户答疑建议
能识别风险话术
能人工审核建议
能标记建议是否使用
能生成复盘报告
界面全中文
话术像翡翠直播间真人表达
10 分钟内能完整演示
```

## 9. 开发纪律

一个月 MVP 阶段必须遵守：

```text
先模拟，后真实
先闭环，后精度
先单直播间，后多直播间
先人工审核，后自动化
先中文体验，后技术炫技
先可演示，后可扩展
```

每天结束前检查：

```text
今天是否让 Demo 更完整？
今天是否减少了一个核心风险？
今天是否有可展示的进展？
今天是否偏离了一个月 MVP？
```

## 10. 立即开始的第一件事

下一步就是创建项目骨架：

```text
frontend：Vue 3 + Vite + TypeScript
backend：FastAPI
docs：迁移当前中文文档
data：准备翡翠样例数据
```

项目启动后，第一天只追求一个结果：

```text
前端页面能打开，后端接口能返回健康状态。
```
