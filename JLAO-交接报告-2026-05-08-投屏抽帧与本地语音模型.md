# JLAO 交接报告 — 2026-05-08（投屏、抽帧与本地语音模型）

## 一、当前产品形态确认

当前采用：

```text
前端：服务器域名
  https://jlao.szkakayiduo.com

本地后端：插手机的电脑
  http://127.0.0.1:8000

手机能力：
  adb / scrcpy / 1FPS 抽帧 / 后续本地语音识别
```

也就是说：

- 本地不需要再启动前端 `npm run dev`。
- 本地只需要启动后端。
- 浏览器打开服务器域名即可。
- 前端默认调用本机后端 `http://127.0.0.1:8000`。

本地后端启动：

```cmd
cd /d D:\JLAO\backend
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

---

## 二、今天已确认跑通

### 1. 服务器前端已可访问

访问：

```text
https://jlao.szkakayiduo.com
```

### 2. 手机截图抽帧已跑通

现象：

- 左侧 / 中间区域能看到手机截图持续更新。
- 说明：

```text
服务器前端
-> 本地后端
-> adb
-> 手机 screencap
-> 压缩 JPG
-> WebSocket 推送
-> 前端显示
```

链路已经通。

### 3. scrcpy 投屏已跑通

现象：

- 点击「载入手机端」后，可以启动 scrcpy 原生投屏窗口。
- 说明本地后端调用 `scrcpy.exe` 成功。

---

## 三、今天已完成的代码改动

### 1. 前端默认 API 改为本地后端

修改文件：

- `frontend/src/api/client.ts`

当前逻辑：

```ts
API_BASE = localStorage api 覆盖值 || http://127.0.0.1:8000
```

仍支持临时覆盖：

```text
https://jlao.szkakayiduo.com?api=http://127.0.0.1:8000
```

但正常访问域名时已经默认走本地后端。

---

### 2. 域名统一

修改文件：

- `deploy/nginx-jlao.conf`
- `backend/app/main.py`
- `deploy/deploy.ps1`

目标域名：

```text
https://jlao.szkakayiduo.com
```

IP 访问会跳转到域名：

```text
47.120.41.143 -> https://jlao.szkakayiduo.com
```

后端 CORS 已加：

```text
http://jlao.szkakayiduo.com
https://jlao.szkakayiduo.com
http://JLAO.szkakayiduo.com
https://JLAO.szkakayiduo.com
```

---

### 3. 部署脚本改成轻量包

修改文件：

- `deploy/deploy.ps1`

之前部署包约：

```text
65MB
```

原因是打包了整个 `backend`，包含 `.venv` 等大文件。

现在只打包：

```text
backend/app
backend/requirements.txt
data
frontend/dist
deploy
```

轻量包约：

```text
279KB
```

部署已验证成功。

---

### 4. 去掉重复截图区域

用户反馈：

> 最左边的截图和中间的重复了；投屏放在左边，最左边截图可拿掉。中间“最新截图”卡片也没用。

已改：

- `frontend/src/pages/LiveDashboard.vue`
  - 最左侧不再显示抽帧截图。
  - 最左侧改为投屏说明 + scrcpy 控制 + 状态。
- `frontend/src/components/LiveSourcePanel.vue`
  - 去掉中间“最新截图”卡片。

注意：

- 截图数据仍保留。
- 右侧截图图库 / 商品识别链路仍可继续使用。
- 只是去掉重复展示。

---

### 5. 载入手机端自动启动投屏 + 抽帧

修改文件：

- `frontend/src/pages/LiveDashboard.vue`

当前逻辑：

```text
点击「载入手机端」
-> store.start()
-> startScrcpySession(defaultPhoneSerial)
-> startPhoneCaptureSession(defaultPhoneSerial)
```

默认设备：

```text
3AF9K24227080668
```

---

### 6. 本地语音识别方向

用户确认：

> 声音处理用本地模型吧

已新增：

- `backend/app/services/local_stt_service.py`
- `backend/requirements-local-stt.txt`
- `JLAO-本地语音识别配置.md`

已修改：

- `backend/app/ws/stt_ws.py`

当前 STT 默认：

```text
STT_PROVIDER=local
```

阿里云仍可回退：

```text
STT_PROVIDER=aliyun
```

---

## 四、本地 faster-whisper 安装状态

用户要求安装：

```cmd
python -m pip install -r backend\requirements-local-stt.txt
```

第一次失败：

```text
Permission denied: Temp pip-unpack...
```

原因：

- Windows 临时目录权限 / 沙箱权限问题。

后改为沙箱外安装，并使用：

```powershell
python -m pip install --user --no-cache-dir -r backend\requirements-local-stt.txt
```

结果：**安装成功**。

成功安装包包括：

```text
faster-whisper==1.1.1
ctranslate2==4.7.1
onnxruntime==1.25.1
av==17.0.1
huggingface-hub==1.14.0
tokenizers==0.23.1
```

安装路径为当前用户 Python：

```text
C:\Users\Administrator\AppData\Roaming\Python\Python312\...
```

有 PATH 警告，但不影响 Python import：

```text
Scripts is not on PATH
```

后续建议验证：

```cmd
python -c "import faster_whisper; print('ok')"
```

---

## 五、当前还没完成 / 需要接班继续

### 1. 最新 UI 改动尚未部署

最后一轮修改包括：

- 去掉左侧重复截图
- 去掉中间“最新截图”卡片
- 本地 STT 代码

这些改动本地已写，但还需要再次：

```powershell
powershell -ExecutionPolicy Bypass -File deploy\deploy.ps1
```

部署时需交互输入两次服务器密码：

```text
[REDACTED_SERVER_PASSWORD]
```

部署包已是轻量包，约 279KB，上传很快。

---

### 2. 本地语音模型还没实际跑通

虽然 `faster-whisper` 已安装成功，但还没完成：

- 模型首次下载 / 指定本地模型目录
- 后端启动后实际识别测试
- 前端语音链路端到端验证

当前默认模型配置：

```text
LOCAL_STT_MODEL=small
LOCAL_STT_DEVICE=cpu
LOCAL_STT_COMPUTE_TYPE=int8
LOCAL_STT_LANGUAGE=zh
LOCAL_STT_CHUNK_SECONDS=4
```

风险：

- 如果没有本地模型，第一次运行会尝试联网从 Hugging Face 下载。
- 国内网络可能失败。

建议：

1. 准备本地模型目录，例如：

```text
D:\models\faster-whisper-small
```

2. 启动前设置：

```cmd
set LOCAL_STT_MODEL=D:\models\faster-whisper-small
set STT_PROVIDER=local
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

---

### 3. 手机声音还没直接接入

当前音频说明：

- scrcpy 视频投屏已经可用。
- 手机内部声音还没直接进入 STT。
- 当前 STT 音频仍依赖浏览器采集：
  - 麦克风
  - 标签页音频
  - 或后续单独做本地音频采集助手

后续需要明确声音来源：

```text
方案 A：电脑麦克风收主播声音
方案 B：采集系统声卡输出
方案 C：scrcpy 音频流 / sndcpy / adb 录音
方案 D：桌面版 Electron 统一采集
```

---

## 六、建议接班操作顺序

### Step 1：验证本地依赖

```cmd
cd /d D:\JLAO
python -c "import faster_whisper; print('ok')"
```

### Step 2：部署最新 UI/STT 代码

```powershell
cd D:\JLAO
powershell -ExecutionPolicy Bypass -File deploy\deploy.ps1
```

密码：

```text
[REDACTED_SERVER_PASSWORD]
```

需要输入两次：

- scp 上传
- ssh 安装

### Step 3：本地启动后端

```cmd
cd /d D:\JLAO\backend
set STT_PROVIDER=local
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

如果有本地模型：

```cmd
set LOCAL_STT_MODEL=D:\models\faster-whisper-small
```

### Step 4：打开前端

```text
https://jlao.szkakayiduo.com
```

点击：

```text
载入手机端
```

确认：

- scrcpy 原生窗口弹出
- 抽帧继续更新
- 左侧不再重复显示截图
- 中间“最新截图”卡片消失

### Step 5：测试语音

在页面里开启音频采集 / STT。

如果失败，看前端错误提示，常见错误：

```text
本地语音识别模型加载失败
本地语音识别未安装
模型下载失败
```

---

## 七、当前涉及文件清单

### 新增

```text
backend/app/services/local_stt_service.py
backend/requirements-local-stt.txt
JLAO-本地语音识别配置.md
JLAO-交接报告-2026-05-08-投屏抽帧与本地语音模型.md
```

### 修改

```text
backend/app/main.py
backend/app/ws/stt_ws.py
deploy/deploy.ps1
deploy/nginx-jlao.conf
frontend/src/api/client.ts
frontend/src/components/LiveSourcePanel.vue
frontend/src/pages/LiveDashboard.vue
frontend/src/styles/global.css
```

---

## 八、Git / 部署状态

最后查看时工作区仍有未提交改动。

建议部署验证后提交：

```cmd
git add .
git commit -m "feat: use local stt and refine phone dashboard"
git push origin main
```

---

## 九、重要提醒

1. 服务器只放前端和云端服务。
2. 手机能力必须跑在插手机的本地电脑。
3. 本地后端必须启动，否则服务器前端无法投屏/抽帧。
4. 当前默认 API 是：

```text
http://127.0.0.1:8000
```

5. 后续做 Electron 桌面版后，可以把前端、本地后端、adb、scrcpy、本地 STT 统一打包成一个软件。

