# JLAO Agent 上下文

---

## 2026-06-09 会话备忘

完整记录见：`2026-06-09-chat-memo.md`。

### 本地前后端结论

- 当前如果前后端都在本地跑，主入口应使用 `http://127.0.0.1:5173`。
- 本地后端入口是 `http://127.0.0.1:8000`。
- 不建议再用线上 `https://jlao.szkakayiduo.com` 去访问本机 `http://127.0.0.1:8000`，浏览器容易因为 HTTPS 到 HTTP、Private Network Access、跨域等原因拦截。
- 线上网站仍适合演示服务器功能、部署静态前端、或未来做公网统一入口；本地硬件相关能力以本地前端 + 本地后端为主。

### 今日已改前端

- `frontend/src/api/baseResolver.ts`
  - 本地开发默认回到 `http://127.0.0.1:8000`。
  - 避免本地 Vite 页面默认把 API 指到 `http://127.0.0.1:5173`。
- `frontend/vite.config.ts`
  - 已添加 Vite dev proxy：
    - `/api` -> `http://127.0.0.1:8000`
    - `/health` -> `http://127.0.0.1:8000`
    - `/uploads` -> `http://127.0.0.1:8000`
    - `/ws` -> `ws://127.0.0.1:8000`
- 已执行 `npm run build`，前端构建通过。

### 检查结果摘要

- 前端：
  - `npm ci` 已安装依赖。
  - `npm run build` 通过。
  - `node --test tests\*.mjs` 有 2 个静态测试失败，主要是测试期望与当前代码文案/音频配置不一致。
- 后端：
  - 已安装 `backend/requirements-filtered.txt` + `pytest`。
  - 未安装完整 GPU/Torch/OCR/Ultralytics 依赖。
  - `python -m pytest tests` 结果为 `128 passed, 10 failed`。
  - 失败集中在翡翠文本分类、YOLO 未配置提示、scrcpy 扫描函数、Native STT 音频源和 adb 路径。

### 四台电脑训练方案

- 4 台电脑可以用同一套程序代码，但每台电脑必须有自己的身份。
- 建议每台电脑本地 `.env` 配置：

```env
JLAO_WORKER_ID=pc-a
JLAO_WORKER_NAME=客厅4090
JLAO_DATA_ROOT=D:\JLAO\data\workers\pc-a
JLAO_RUN_ROOT=D:\JLAO\runs\workers\pc-a
```

- 四台电脑分别使用：

```text
pc-a
pc-b
pc-c
pc-d
```

- 数据策略：
  - 4 台电脑的数据全部汇总成总训练集。
  - 不要只选其中一台电脑的数据。
  - 汇总后统一去重、清洗、标注、切 train/val/test。
- 模型策略：
  - 4 台电脑可以并行训练不同实验。
  - 最终用同一验证集/测试集评估。
  - 正式上线通常只选一个稳定主模型，输出到 `models/jade-yolo.pt`。
  - 其他模型保留为备份和对比。

### 重启后优先事项

1. 用 `http://127.0.0.1:5173` 验证本地前端。
2. 用 `http://127.0.0.1:8000/health` 验证本地后端。
3. 给 4 台电脑分别配置 `JLAO_WORKER_ID`。
4. 后续可补脚本：自动读取 `JLAO_WORKER_ID` 并生成采集、训练、模型输出路径。
## 项目概述

视频号翡翠直播间AI助手系统。为主播、观众、运营提供全链路翡翠识别与直播辅助能力。

## 产品定位

- **主播端 - 实时辅助讲解**：直播画面实时识别翡翠，自动弹出种水、颜色、器型、价格参考，辅助主播讲货
- **观众端 - 买家识别工具**：观众截图或上传翡翠图片，系统返回识别结果和购买建议
- **运营后台 - 内容审核/质检**：直播内容翡翠品质审核、假货识别、主播话术合规检查

## 技术架构（本地算力）

```
┌─────────────────────────────────────────┐
│           用户本地电脑                    │
│                                         │
│  ┌─────────────┐    ┌─────────────┐   │
│  │   浏览器     │◄──►│   本地后端    │   │
│  │  (Vue3)     │    │ (FastAPI)   │   │
│  └─────────────┘    └──────┬──────┘   │
│                            │          │
│  ┌─────────────────────────┼────────┐ │
│  │  USB 投屏（scrcpy）      │        │ │
│  │  语音转写（FunASR）◄─────┘        │ │
│  │  图像AI（YOLO+VLM+OpenCV）         │ │
│  │  弹幕OCR（PaddleOCR/RapidOCR）     │ │
│  └───────────────────────────────────┘ │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│        服务器（47.120.41.143）           │
│                                         │
│     ┌─────────────┐    ┌───────────┐  │
│     │   Nginx     │    │ PostgreSQL │  │
│     │  （前端）    │    │ （数据库）  │  │
│     └─────────────┘    └───────────┘  │
└─────────────────────────────────────────┘
```

数据流：
1. 前端（服务器 Nginx）→ 本地后端（FastAPI，HTTPS）
2. 本地后端 → 服务器数据库（PostgreSQL）

## 关键约束

- **视频号直播间为竖屏** — 所有画面处理、YOLO训练、VLM分析必须适配竖屏比例
- 本地算力为主 — GPU推理（YOLO/VLM/OCR/STT）全部在本地运行
- USB设备无法远程 — scrcpy投屏必须在本地
- 后端需要连接远程服务器数据库

## 视频号直播间 ROI 标定数据

### 手播 ROI（已标定）

**主播手持翡翠展示**模式，基于6张真实直播间截图标定：

```
x: 5%  ~ 95%   (宽度 90%)
y: 15% ~ 55%   (高度 40%)
```

**竖屏 1080×1920 换算：** x=[54, 1026], y=[288, 1056]
- ROI 面积占全图：36%
- 计算量减少：64%

**使用方式：** 帧提取后先按此 ROI 裁剪，再送入 YOLO/VLM 识别。

### 面播 ROI（待标定）

**面播**指主播坐桌前，翡翠放桌面/垫子上展示（非手持）。
- 翡翠展示区通常更靠下（桌面在画面中下部）
- 需另外标定，待用户提供面播参考图后补充

## 货品面积阈值（YOLO 后处理过滤）

基于6张手播标定图，**翡翠货品占 ROI 红框**的比例：

| 图 | 货品 | 货品/红框 | 判定 |
|---|---|---|---|
| 1 | 两枚戒指 | ~21% | 有效 |
| 2 | 一对耳钉 | ~34% | 有效 |
| 3 | 红翡戒指 | **~15%** | **最小有效货品** |
| 4 | 紫罗兰圆牌 | ~76% | 有效 |
| 5 | 手镯（竖放）| ~11% | 个例，主播会转至平放 |
| 6 | 手镯（平放）| ~40% | 有效 |

**阈值规则：**
```
检测框面积 / 红框面积 ≥ 15% → 保留（有效翡翠）
检测框面积 / 红框面积 < 15% → 丢弃（噪声：手指、背景、UI）
```

**设定依据：**
- 图3红翡戒指（~15%）是6张图中最小的有效货品
- 图5竖放手镯（~11%）是个例，实际直播中会调整至平放（图6，~40%）
- 低于15%的检测目标通常是手指关节、背景杂物、商品信息浮层等

**应用：** YOLO 推理后，先过滤低置信度框，再按面积比过滤。该阈值仅适用于手播 ROI。

## 核心识别维度

1. **基础属性**：种水（玻璃种/冰种/糯种等）、颜色（绿/紫/黄/白等）、器型、题材
2. **瑕疵识别**：裂纹、棉、黑点、杂质
3. **价格评估**：基于识别结果给出市场参考价区间
4. **真伪鉴别**：A货/B货/C货/D货（需要进一步扩展）

## 已有基础设施

- **多模态融合引擎**（`jade_multimodal_service.py`）：YOLO + VLM + OCR + STT + OpenCV 加权融合
- **YOLO检测**：14类翡翠器型（手镯、珠串、蛋面、观音、佛公等），支持自定义 `jade-yolo.pt`
- **VLM视觉分析**：本地 transformers 或 HTTP API（Ollama/OpenAI兼容），两阶段：VLM提取形态 → OpenCV提取颜色
- **OCR**：Tesseract（`chi_sim` + `eng`），过滤翡翠关键词（种水、颜色、价格、mm等）
- **STT**：FunASR（本地）+ 原生手机音频捕获
- **反馈学习闭环**：弱样本自动收集 → 人工标注 → YOLO训练 → 评估
- **前端**：`ProductLibrary.vue` 已包含完整识别UI（实时分析、反馈提交、标注任务、训练启动、评估）

## 关键文件

- `backend/app/services/jade_multimodal_service.py` — 融合大脑
- `backend/app/services/jade_vlm_service.py` — VLM + CV 识别
- `backend/app/services/jade_yolo_service.py` — 目标检测
- `backend/app/api/products.py` — 翡翠API（分析、反馈、训练、标注）
- `frontend/src/pages/ProductLibrary.vue` — 识别主界面
- `backend/app/db_models.py` — FrameSnapshotRecord / ProductRecord

## 当前需求

做一个**视频号翡翠直播间主播讲货翡翠识别系统**，重点：
- 适配**竖屏**直播间画面
- 主播讲货时，系统实时识别镜头前的翡翠，自动展示种水/颜色/价格参考
- 本地算力运行，低延迟

**TODO：等待用户发送视频号竖屏直播间参考图**
