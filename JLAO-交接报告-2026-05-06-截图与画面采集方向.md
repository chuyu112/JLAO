# JLAO 交接报告 - 截图链路与画面采集方向 - 2026-05-06

## 一、当前结论

今天最大的结论是：JLAO 必须能看见直播画面，否则对翡翠类目价值会明显下降。但画面入口不应该继续押在“抖音网页链接 / 浏览器标签页 / 客户电脑屏幕截图”上。

目前更合理的产品方向是：

```text
手机侧采集画面
-> 上传到 JLAO
-> AI 判断题材、类型、种、水、色、底、工
-> 给主播/场控输出直播话术建议
```

原因：

- 翡翠类目画面就是货品本身，不能只靠语音和商品资料。
- 小直播间多数是手机直接开播，不会有 OBS、直播伴侣、标准推流链路。
- 客户不可能长期配合把直播网页或直播窗口摆在电脑屏幕上给系统截图。
- 抖音网页链接不是视频流，不能稳定交给 FFmpeg 直接抽帧。

## 二、今天已确认跑通的链路

### 1. 手动截图上传链路已跑通

当前稳定链路：

```text
微信截图 / 系统截图
-> JLAO 前端读取剪贴板图片
-> FastAPI 上传图片
-> 后端保存到 uploads/frames/{session_id}
-> OpenCV 基础检测
-> 前端右侧截图卡片显示
```

已验证成功：

- 前端可以读取微信截图剪贴板。
- 后端可以保存图片。
- OpenCV 可以返回清晰度、亮度、画面状态。
- Nginx `/uploads/` 已修复，图片现在返回 `image/jpeg`，不再返回前端 HTML。
- 截图卡片已移动到右侧顶部，用户已确认“可以了，我都看到了”。

相关提交：

```text
3c5949c Improve screenshot paste fallback
b045f91 Proxy uploaded frame images
07a2c91 Move screenshot cards to right panel
```

### 2. 翡翠看图话术能力已人工验证方向

基于用户上传的佛公截图，已经可以按直播表达方式输出：

```text
题材：佛公 / 弥勒佛
类型：冰感淡紫佛公挂件
种：冰种到高冰感
水：水头足
色：淡紫 / 淡紫罗兰
底：底妆比较干净，有轻微白棉/雾感
工：小佛公工还可以，肚子饱满，脸部轮廓清楚
直播话术：冰紫佛公，小件但水头足，肚子透，底子清爽，寓意笑口常开
```

用户反馈：“能说成这样可以了”。  
这说明 JLAO 的核心 AI 输出方向成立。

## 三、今天验证后不适合作为主线的方案

### 1. 抖音直播网页 iframe 嵌入

不可行。

`live.douyin.com` 会拒绝 iframe 连接。网页不能直接嵌到 JLAO 前端里。

### 2. 浏览器标签页 / 窗口采集后前端 canvas 截图

不稳定。

现象：

- 浏览器能播放直播画面。
- 但 JLAO 前端从 `<video>` 或 `ImageCapture.grabFrame()` 抽帧时经常得到黑屏。
- 用户点击“识别当前画面”仍然黑屏。

判断：

- 抖音直播画面可能走特殊视频层、硬件加速或受保护渲染。
- 浏览器允许显示，但不保证允许二次读取像素。

结论：

这条路不能作为主线。

### 3. 本地电脑截屏助手

只能作为调试工具，不适合商业主方案。

缺点：

- 依赖客户电脑屏幕状态。
- 直播窗口必须可见，不能被遮挡。
- 多屏、缩放、窗口移动都会导致截错。
- 客户不可能长期按要求把窗口摆好。
- 小直播间多为手机直接开播，甚至没有电脑直播画面。

结论：

保留为临时调试/兜底，不作为 JLAO 商业架构核心。

### 4. OBS / 直播伴侣 / 推流链路

对部分专业直播间可用，但不适合主流小直播间。

用户明确反馈：OBS 不行，太慢。很多小直播间都是直接手机开播。

结论：

FFmpeg 可以支持标准视频流输入，但不能假设每个客户都有 OBS 或 RTMP/FLV/M3U8。

### 5. 抖音网页链接转视频流

不适合作为 MVP 主线。

区别：

```text
抖音网页链接 = 给浏览器看的页面
真实视频流 = FFmpeg 可读的 m3u8 / flv / rtmp / mp4
```

抖音直播真实流地址通常：

- 登录后动态生成
- 带签名
- 带时效
- 可能绑定 Cookie / Referer / User-Agent
- 稳定性和合规风险都高

结论：

不要承诺“任意抖音网页链接都能分析画面”。

## 四、当前线上功能状态

线上地址：

```text
https://jlao.szkakayiduo.com/live
```

当前线上已可用：

- 登录页 / JLAO 直播中控台
- 直播链接保存
- 标签页音画接入，主要用于声音/STT
- 阿里云实时 STT 链路此前已跑通
- 上传截图识别
- 读取剪贴板截图
- OpenCV 清晰度/亮度/基础状态检测
- 右侧截图卡片显示
- 左侧最新截图预览
- `/uploads/` 图片静态访问修复

当前已关闭：

- 自动截图
- 本地助手每秒循环

当前保留：

- 手动上传截图
- 读取剪贴板截图
- 本地助手截一次，仅作为调试工具

## 五、代码状态

### 已提交到 GitHub 的最近提交

```text
07a2c91 Move screenshot cards to right panel
b045f91 Proxy uploaded frame images
3c5949c Improve screenshot paste fallback
c0fdfa8 Disable automatic screenshot capture
b60f0c6 Set capture interval to one second
9b5602e Add local screen capture helper
```

### 当前本地未提交状态

当前本地还有 FFmpeg 抽帧草稿代码未正式提交：

```text
M backend/app/api/frames.py
M backend/app/main.py
M backend/app/services/frame_service.py
M backend/requirements.txt
?? backend/app/api/ffmpeg_capture.py
?? backend/app/services/ffmpeg_capture_service.py
```

这部分做了什么：

- 引入 `imageio-ffmpeg`，准备用 Python 包自带 ffmpeg 二进制。
- 抽出了 `create_frame_snapshot()`，让上传图片和 FFmpeg 抽帧复用同一套保存/分析/推送逻辑。
- 新增 FFmpeg 抽帧服务草稿：
  - `/api/sessions/{session_id}/ffmpeg-capture/once`
  - `/api/sessions/{session_id}/ffmpeg-capture/start`
  - `/api/sessions/{session_id}/ffmpeg-capture/stop`
  - `/api/sessions/{session_id}/ffmpeg-capture/status`

注意：

- 这部分还没有用真实 m3u8/flv/rtmp 测试。
- 不应该把它包装成“支持抖音网页链接抽帧”。
- 如果继续做，需要只定位为“标准视频流输入能力”。

## 六、后续建议路线

### 第一优先级：手机侧采集方案

小直播间直接手机开播，因此最有意义的画面入口应该在手机侧。

建议 MVP 方向：

```text
手机 A：正常抖音开播
手机 B：JLAO 采集端，对着货品/直播画面低频拍摄
JLAO：每 1-3 秒拿一帧分析翡翠
前端：输出题材、种水色底工、直播话术
```

优点：

- 不碰抖音 App 内部。
- 不依赖电脑屏幕。
- 不需要 OBS。
- 小直播间更容易执行。
- 可以快速验证“AI 看货 + 话术建议”的产品价值。

未来升级：

- Android MediaProjection 授权录屏。
- Android 轻量 App 自动推帧。
- 局域网/云端推帧。
- 多手机、多直播间管理。

### 第二优先级：把翡翠 AI 判断结构固化

建议输出结构：

```text
题材
类型
种：老/嫩、冰糯/冰/高冰等
水：水头足/一般/干
色：颜色、浓淡、分布
底：底妆干净程度、棉、脏、灰
工：雕工、比例、饱满度、细节
直播话术
风险提醒
```

风险措辞：

- 不轻易说“玻璃种”
- 不轻易说“帝王绿”
- 不轻易说“无棉无裂”
- 不直接定价
- 用“看图初判 / 直播画面看 / 以实物和证书为准”

### 第三优先级：FFmpeg 标准视频流输入

仅支持：

```text
m3u8 / flv / rtmp / mp4
```

不支持：

```text
普通抖音网页链接
```

这条能力可以给更专业的直播间或后期扩展用，不作为小直播间 MVP 主入口。

## 七、下一步具体任务建议

1. 暂停继续折腾抖音网页画面采集。
2. 把“截图识别”正式升级为“翡翠识别结果卡片”。
3. 在截图卡片右侧展示：
   - 题材
   - 类型
   - 种
   - 水
   - 色
   - 底
   - 工
   - 直播话术
4. 接入多模态大模型 API，看图生成翡翠判断。
5. 开始设计手机采集端 MVP：
   - 最简单可先做 H5/小 Web 页面拍照上传
   - 再做 Android 原生/轻量 App
6. FFmpeg 草稿代码先保留，但不要作为主线宣传。

## 八、一句话总结

JLAO 的价值不是“截屏工具”，而是“翡翠直播实时看货 + 话术优化系统”。  
画面采集入口应该从客户的手机侧解决，而不是继续依赖抖音网页、OBS 或客户电脑屏幕摆放。
